MappingModel TemplateMapping {
	
	import physical.* from "../../model/physical.etphys"
	import scenario.* from "scenario.room"

	Mapping log_sys -> physical_system {
		SubSystemMapping sub_system_ref -> node {
			ThreadMapping thread -> physical_thread
		}
	}

}